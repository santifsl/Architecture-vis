# Dependencies
/node_modules/

# Build output
/build/

# React Router generated route types
/.react-router/

# Vite
*.local
.vite/

# Environment — VITE_PUTER_WORKER_URL lives here, never commit it.
# .env.example is deliberately allowed through so the required vars are documented.
.env
.env.*
!.env.example

# Vercel
.vercel

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Claude Code's local tool configuration. The skill content itself lives in
# .agents/skills/ and is committed; .claude/ only symlinks it into this machine.
.claude/

# Editors
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
