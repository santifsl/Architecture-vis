# The deployed Puter worker that calls Claude and Gemini. Deploy one with `puter worker deploy <file> <name>` (or right-click a .js file on puter.com and "Publish as Worker") and copy the https://<worker-name>.puter.work URL it gives you.
VITE_PUTER_WORKER_URL=
